function calculateAreaAndPerimeter(firstLine,secondLine){
    let area = firstLine*secondLine;
    console.log(area);
    let perimeter = 2*firstLine + 2*secondLine;
    console.log(perimeter);
}

calculateAreaAndPerimeter(2,2);