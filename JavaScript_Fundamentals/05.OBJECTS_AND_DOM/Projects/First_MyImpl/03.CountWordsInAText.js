function countWords(input){
    let arr = input.split(' ');

    for(let i = 0; i < arr.length; i++){
        if(i.include());
    }

    console.log(arr);

}

countWords("Far too slow, you're far too slow.");